= render 'shared/errors', model: @tag
= form_with model: @tag, local: true do |f|
  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control bg-dark'
  .form-group.actions
    = f.submit nil, class: 'btn btn-primary'
= link_to 'Back', tags_path
