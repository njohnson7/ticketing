%h2= @project.name
%p= @project.description
%p
  = link_to 'Edit', edit_project_path(@project)
  = link_to 'Delete', project_path(@project), method: :delete
%h2 Tickets
%table.table.table-dark
  %thead
    %tr
      %th Name
      %th.text-center Assignee
      %th.text-center Status
      %th.text-center Tags
